using System.IO;

namespace _01.BashSoft
{
    //Holds the date about the current sesion.
    public static class SessionData
    {
        public static string currentPath = Directory.GetCurrentDirectory();
    }
}
